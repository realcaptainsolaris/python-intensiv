"""
(LEICHT)
**Aufgabe 1: Verwenden von `map()`, um Zahlen zu quadrieren**

Du erhältst eine Liste von Ganzzahlen. Deine Aufgabe ist es, ein Python-Programm zu schreiben, das `map()` verwendet, um jede Zahl in der Liste zu quadrieren und eine neue Liste mit den quadrierten Werten zu erstellen.
"""
numbers = [1, 2, 3, 4, 5]
# Verwende map(), um die Zahlen zu quadrieren

# Gib die ursprüngliche Liste und die quadrierten Zahlen aus
print("Ursprüngliche Liste:", numbers)


"""
(LEICHT)
**Aufgabe 2: Verwenden von `filter()`, um gerade Zahlen zu finden**

Du erhältst eine andere Liste von Ganzzahlen. Deine Aufgabe ist es, ein Python-Programm zu schreiben, das `filter()` verwendet, um nur die geraden Zahlen aus der ursprünglichen Liste zu finden und eine neue Liste mit diesen geraden Zahlen zu erstellen.

numbers = [10, 15, 20, 25, 30]

"""
numbers = [10, 15, 20, 25, 30]


"""
(MITTEL)
3. Aufgabe
Für eine zusätzliche Herausforderung kannst du `map()` und `filter()` kombinieren, um die folgende Aufgabe zu lösen:

Du erhältst eine Liste von Ganzzahlen. Deine Aufgabe ist es, ein Python-Programm zu schreiben, das sowohl `map()` als auch `filter()` verwendet, um eine neue Liste zu erstellen, die die Quadrate nur der geraden Zahlen aus der ursprünglichen Liste enthält.

1. Erstelle eine Liste von Ganzzahlen (zum Beispiel `[2, 3, 4, 5, 6]`).
2. Verwende `filter()`, um die geraden Zahlen in der Liste zu finden.
3. Verwende `map()`, um das Quadrat jeder der in Schritt 2 gefundenen geraden Zahlen zu berechnen.
"""
numbers = [2, 3, 4, 5, 6]
